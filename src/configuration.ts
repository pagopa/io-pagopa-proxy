/**
 * Italia PagoPA Proxy
 * Cittadinanza Digitale PagoPA services
 */

// App configuration
export const CONFIG = {
  CONTROLLER: {
    PORT: 3000,
    ROUTES: {
      INDEX: "/",
      WALLETS: "/wallets/",
      CARDS: "/cards/",
      TRANSACTIONS: "/transactions/"
    }
  }
};
